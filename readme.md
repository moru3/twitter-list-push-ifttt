This app checks a twitter list since in a hour, and if it finds tweets, send push notifications using IFTTT push notification endpoint.
This app runs on AWS Lamda with CloudWatch Scheduler(per a hour).

- language : python3
- libraries : twitter
